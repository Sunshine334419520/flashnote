# README 截图指南

## 截图清单（5 张）

### 准备

1. 打开 FlashNote，确保有一定数量的示例笔记（API Key、书签、命令、文本各几条）
2. 设置浅色主题，界面更清晰
3. 隐藏敏感数据的真实内容（用 mock 数据）

---

### 1. Hero 图 — Alt+Space 快捷窗口

**截图内容：**
- 按 `Alt+Space` 弹出快捷窗口
- 输入框中有文字：`docker compose`
- 下方显示 2-3 条搜索结果（包含不同类型卡片）
- 底栏显示 `Enter ↵ AI 搜索 / 创建`

**文件：** `docs/screenshots/hero.png`
**尺寸建议：** 680×350

**AI 标注 prompt：**
```
给这张截图添加标注，不要修改原图，只在四周空白区域添加标注线+文字：

界面上方输入框 → 标注①："输入即搜，Enter 走 AI"
搜索框右侧 "AI ↵" 标识 → 标注②："快捷键提示"
下方搜索结果行 → 标注③："类型识别 · 一键复制"
底栏提示文字 → 留原样不标注

标注样式：圆角矩形边框，数字用彩色圆形，线条细灰，文字用截图内的字体大小。整张图保持干净克制。
```

---

### 2. 卡片墙

**截图内容：**
- 主窗口完整界面
- 卡片墙展示 4-5 种类型卡片
- 顶部命令栏可见

**文件：** `docs/screenshots/card-wall.png`
**尺寸建议：** 780×500

**AI 标注 prompt：**
```
给这张截图添加标注，不要修改原图：

左侧 API Key 卡片 → 标注①："API Key · 一键复制"
右侧命令卡片 → 标注②："Command · 复制命令"
左下书签卡片 → 标注③："Bookmark · 打开链接"
右下文本卡片 → 标注④："Text · 展开全文"
每张卡片底部的类型徽章用箭头指向

标注样式同上，保持简洁。
```

---

### 3. AI 命令

**截图内容：**
- 主窗口命令框输入：`/add sk-xxx123 my deepseek api key for production`
- 或者展示 `/search docker` 的搜索结果

**文件：** `docs/screenshots/ai-command.png`
**尺寸建议：** 780×400

**AI 标注 prompt：**
```
给这张截图添加标注，不要修改原图：

命令框中的 /add 命令 → 标注①："自然语言指令"
AI 解析后创建的卡片（如果有）→ 标注②："AI 自动分类为 API Key"
卡片中的类型徽章 → 标注③："类型自动识别"

如果没有创建后的效果，截取搜索场景：
搜索框 /search docker → 标注①："语义搜索"
匹配的搜索结果 → 标注②："AI 精排，最相关在前"
```

---

### 4. 设置页

**截图内容：**
- 设置页面（可从主窗口 Settings 进入）
- 显示主题、语言、快捷键、AI 服务商配置

**文件：** `docs/screenshots/settings.png`
**尺寸建议：** 720×500

**AI 标注 prompt：**
```
给这张截图添加标注，不要修改原图：

快捷键区域下拉菜单 → 标注①："自定义全局快捷键"
AI 服务商列表 → 标注②："多服务商支持"
"设为使用中"按钮 → 标注③："一键切换活跃服务商"
服务商类型标签（Anthropic/DeepSeek）→ 不用标注，已清晰
```

---

### 5. 状态栏 AI 记录

**截图内容：**
- 主窗口底部状态栏
- 点击 `✦ AI`，弹出操作记录面板
- 面板中显示几条 AI 操作记录（成功+失败）

**文件：** `docs/screenshots/status-bar.png`
**尺寸建议：** 400×300

**AI 标注 prompt：**
```
给这张截图添加标注，不要修改原图：

状态栏 AI 图标 → 标注①："AI 操作记录入口"
面板中的失败记录（红色） → 标注②："失败记录 · 一键重试"
面板中的成功记录（绿色圆点） → 标注③："操作成功 · 耗时显示"

重点突出失败记录的重试按钮。
```

---

## 替换 README 中的占位符

截图完成后，将文件放入 `docs/screenshots/` 目录，README 中的图片引用即可生效：

```
docs/screenshots/
  hero.png
  card-wall.png
  ai-command.png
  settings.png
  status-bar.png
```

无需修改 README 文件，路径已经写好了。
